@model Future_Generation.Models.StudentCourses

@{
    ViewData["Title"] = "Details";
}

<div class="container mt-4">
    <h1 class="text-center">Details</h1>

    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Student Course</h4>
            <hr />

            <dl class="row">
                <dt class="col-sm-3">Student</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.Student.Name)</dd>

                <dt class="col-sm-3">Course</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.Course.CourseName)</dd>
            </dl>

            <div class="text-center">
                <a asp-action="Edit" asp-route-id="@Model?.Id" class="btn btn-warning">Edit</a>
                <a asp-action="Index" class="btn btn-secondary">Back to List</a>
            </div>
        </div>
    </div>
</div>

